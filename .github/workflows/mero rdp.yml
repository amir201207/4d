name: "Windows 11 with Auto-Restart"

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"  # إعادة التشغيل كل 6 ساعات

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360  # 6 ساعات كحد أقصى

    steps:
    - name: Download RDP setup
      run: |
        Invoke-WebRequest -Uri "https://raw.githubusercontent.com/iamshahzaib/EnigMano/main/EnigMano-Instance.ps1" -OutFile "EnigMano-Instance.ps1"

    - name: Run RDP
      run: |
        powershell -ExecutionPolicy Bypass -File ".\EnigMano-Instance.ps1" -username "amir20120" -password "amiraliA@1" -ngrok_token "${{ secrets.NGROK_TOKEN }}"
