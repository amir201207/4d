name: Windows RDP via Ngrok
on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Download and Install Ngrok
      run: |
        Invoke-WebRequest https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip -OutFile ngrok.zip
        Expand-Archive ngrok.zip -DestinationPath ngrok
        .\ngrok\ngrok.exe config add-authtoken $Env:NGROK_AUTH_TOKEN
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}

    - name: Enable RDP
      run: |
        net user amir20120 amiraliA@1 /add
        net localgroup administrators amir20120 /add
        reg add "HKLM\SYSTEM\CurrentControlSet\Control\Terminal Server" /v fDenyTSConnections /t REG_DWORD /d 0 /f
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

    - name: Start Ngrok Tunnel and show TCP Address
      run: |
        Start-Process -FilePath .\ngrok\ngrok.exe -ArgumentList "tcp 3389 --log=stdout" -PassThru | Out-Null
        Start-Sleep -Seconds 10
        $tunnels = Invoke-RestMethod http://127.0.0.1:4040/api/tunnels
        $tcpTunnel = $tunnels.tunnels | Where-Object { $_.proto -eq "tcp" }
        Write-Host "====================================="
        Write-Host "Use this address in Remote Desktop:"
        Write-Host $tcpTunnel.public_url
        Write-Host "Username: amir20120"
        Write-Host "Password: amiraliA@1"
        Write-Host "====================================="
