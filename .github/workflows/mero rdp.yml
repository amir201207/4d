name: Free Windows RDP with Cloudflare

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - name: Install Cloudflare Tunnel
        run: |
          Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe
          Move-Item cloudflared.exe C:\Windows\System32\cloudflared.exe

      - name: Enable RDP
        run: |
          net user amir20120 amiraliA@1 /add
          net localgroup administrators amir20120 /add
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1

      - name: Start RDP Tunnel
        run: |
          cloudflared tunnel --url tcp://localhost:3389 --no-autoupdate > tunnel.log 2>&1 &
          Start-Sleep -Seconds 10
          type tunnel.log

      - name: Show Connection Info
        run: |
          echo "====================================="
          echo "استخدم هذا العنوان في برنامج Remote Desktop"
          echo "اسم المستخدم: amir20120"
          echo "كلمة المرور: amiraliA@1"
          echo "====================================="
          type tunnel.log
