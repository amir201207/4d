version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.10
    steps:
      - checkout
      - run: pip install -r requirements.txt
      - run: pytest

  deploy:
    docker:
      - image: cimg/python:3.10
    steps:
      - checkout
      - run: echo "Deploying to server..."
      # هنا ممكن تكتب أوامر SSH أو استخدام أدوات النشر

workflows:
  build_test_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
